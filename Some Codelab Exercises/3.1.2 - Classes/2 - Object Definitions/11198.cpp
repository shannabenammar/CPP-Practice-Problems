/**
 * Objects of the `Window` class require a width (integer) and a
 * height (integer) be specified (in that order) upon definition.
 * 
 * Define an object named `window`, of type `Window`, corresponding
 * to a 80 x 20 window.
 */

Window window(80, 20);