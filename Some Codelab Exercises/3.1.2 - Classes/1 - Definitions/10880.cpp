/**
 * Write the interface (.h file) of a class `Acc1` containing
 * no constructors, functions, or instance variables.
 * (Note, the last character of the class name is "one" not "ell")
 */

class Acc1
{
    
};