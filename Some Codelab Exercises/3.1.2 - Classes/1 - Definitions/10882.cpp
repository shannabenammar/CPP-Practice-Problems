/**
 * Write the interface (.h file) of a class `Acc2` containing
 * - A data member named `sum` of type integer
 * - A constructor accepting no parameters
 * - A function named `getSum` that accepts no parameters and
 * returns an integer 
 */

class Acc2
{
    private:
        int sum;
    public:
        Acc2();
        int getSum();
};