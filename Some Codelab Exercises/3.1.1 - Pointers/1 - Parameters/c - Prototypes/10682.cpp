/**
 * Write a statement that declares a prototype for a function `zeroIt`,
 * which can be used as follows:
 * 
 * int x = 5;
 * zeroIt(&x);
 * // x is now equal to 0
 */

// Prototype of 10672
void zeroIt(int *x);