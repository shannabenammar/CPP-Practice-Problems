/**
 * Write a statement that declares a prototype for a function `minMax`,
 * which has five parameters. The first three parameters are integers.
 * The last two are parameters that are set by the function to the
 * largest and smallest of the values of the first three parameters.
 * The function does not return a value.
 */

// Prototype of 10676
void minMax(int x, int y, int z, int *big, int *small);