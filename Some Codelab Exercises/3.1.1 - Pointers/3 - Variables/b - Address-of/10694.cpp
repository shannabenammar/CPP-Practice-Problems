/**
 * Assume that an `int` variable `counter` has already been declared.
 * Assume further a variable `counterPointer` of type "pointer to `int`"
 * has also already been declared.
 * 
 * Write a statement that makes `counterPointer` "point" to `counter`.
 */

counterPointer = &counter;