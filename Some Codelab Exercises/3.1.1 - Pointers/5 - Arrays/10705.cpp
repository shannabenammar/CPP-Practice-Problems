/**
 * Assume that `ip` has been declared to be a pointer to `int`
 * and that `enrollment` has been declared to be an array of
 * 20 elements.
 * 
 * Write a statement that makes `ip` point to the first
 * element in the array.
 */

// Since ip is a pointer we must assign it the address of enrollment[0]
ip = &enrollment[0];